<h1><%= @task.id %>のタスク詳細</h1>

<table class="table table-condensed table-bordered">
  <tr>
    <th>No.</th>
    <td><%= @task.id %></td>
  </tr>
  <tr>
    <th>タスク名</th>
    <td><%= @task.content %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @task.status %></td>
  </tr>
</table>


<%= link_to "タスクの変更", edit_task_path(@task), class: "btn btn-default" %>

<%= link_to "タスクの削除", @task, method: :delete, data: { confirm: "本当に削除してよろしいですか？" }, class: "btn btn-danger" %>